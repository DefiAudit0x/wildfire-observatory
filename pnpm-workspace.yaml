overrides:
  'uuid@<11.1.1': ^11.1.1
  # Dependabot 2026-09: fast-uri 4x HIGH (host confusion / SSRF family,
  # GHSA-5jgf-p345-68v8 et al.) and qs 2x MEDIUM (bracket-key comma
  # bypass + isBuffer DoS). Pins ride in the workspace settings file —
  # pnpm 11 no longer reads the `pnpm` field in package.json.
  'fast-uri@<3.1.6': 3.1.6
  'qs@<6.16.0': 6.16.0

allowBuilds:
  esbuild: true
  '@firebase/util': false
  '@google/genai': false
  '@scarf/scarf': false
  '@sentry/cli': false
  protobufjs: false
  re2: false
