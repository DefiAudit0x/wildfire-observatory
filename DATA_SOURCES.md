# DATA_SOURCES.md — مصادر بيانات المرصد الجزائري لحرائق الغابات والكوارث

وثيقة رسمية تصف مصدر كل بيانات تعرضها المنصة، وتأخرها الزمني، وسلوكها عند تعطّل المصدر.

## 1. بقع حرائق الأقمار — NASA FIRMS (MODIS / VIIRS)

- **النقطة**: `GET /api/satellite-data` (`server/routes/satellite.ts`)
- **المصدر**: NASA FIRMS - FIRMS API للبقع الحرارية:
  - `VIIRS_SNPP_NRT` و `MODIS_NRT` (مخزون NRT خلال ~3 ساعات من الرصد)
  - نطاق شمال أفريقيا: `18..38N / -17..25E`، آخر 3 أيام
- **التأخر الزمني**: **شبه فوري (near-real-time)** — يصل إلى بضع ساعات من لحظة الرصد؛ **ليس حياً بالثانية**.
- **المسار**: إما مباشرة عبر `NASA_FIRMS_KEY` (يشترط تبييض IP الخادم في حساب FIRMS)، أو عبر وكيل Cloudflare Worker (`FIRMS_BASE_URL` يشير إلى `*.workers.dev`).
- **التبعية المُعدَّلة**: البقع Hellfire/FRP تُحوَّل إلى درجة ثقة (h/high→95، l/low→45، غيرها→80) ثم تُخصَّص لكل ولاية عبر `determineWilayaByCoords`.
- **الاندماج**: نفس البقعة في MODIS وVIIRS (ضمن 0.05°) تُدمج في نقطة واحدة أعلى ثقة.
- **عند التعطّل**: مهلة 15 ثانية؛ عند فشل الطلب أو غياب المفتاح تُعاد **قائمة فارغة** — سماء صادقة بلا بؤر. (منذ v2.3.0 لا توجد بقع تجريبية وهمية إطلاقاً؛ وسم `isFallback` لم يعد يُنتَج.)

## 2. بلاغات المواطنين + التحقق

- **النقطة**: `GET /api/reports` + `POST /api/reports`
- **المصدر**: Firestore (مجموعة `reports`) حصراً. **منذ v2.3.0 لا توجد بذرة بلاغات تجريبية** — قاعدة فارغة تعني صفر حرائق معروضة، وتعطّل قاعدة البيانات يعني 503، وليس بلاغات وهمية.
- **التحقق**: Gemini Vision لصور البلاغ (ثقة ≥75% تأكيد)، والإجماع البشري (5+ تأكيدات)، وقرب بقعة FIRMS (≤3كم) — راجع `README`.
- **الخصوصية (منذ v2.4.0 — فصل الهوية)**: هوية المبلّغ (الاسم/الهاتف/شارة/معرّف الجهاز) لا تُكتب أصلاً في المستند العلني — تُحفظ في `reportPrivate` (الخادم فقط)، والصورة في `reportImages` وتُجلب عند الطلب عبر `GET /api/reports/:id/image`. القائمة العامة تحمل `hasImage: true` بدل جسم الصورة، ولا تحمل حقلاً هوياً واحداً. صفوف ما قبل v2.4.0 تُنظَّف عند القراءة وتُرحَّل تلقائياً إلى الأجزاء المقسومة.
- **تكرار الإرسال**: بلاغ بمفتاح `clientGeneratedId` يُعاد منه المحفوظ الأول — منذ v1 للبلاغات.

## 3. النداءات SOS

- **النقطة**: `POST /api/sos` + `GET /api/sos`
- **المصدر**: Firestore (`trappedSos` — لقطة بدون الصوت) + ذاكرة الخادم (الصوت بصيغة base64، سقف 200 عنصر / 12 ساعة).
- **التشفير**: مفاتيح SOS/S0S مخصصة وتناوب وإثبات عمل في شبكة Mesh — راجع `server/routes/sos.ts`.
- **تكرار الإرسال (منذ v2.4.0 — F4)**: `clientGeneratedId` اختياري في نداء SOS — إعادة الإرسال بعد انقطاع شبكة تعيد **أول نداء محفوظ** (سجل `sosIdempotency` على الخادم) ولا تُنشئ نداءً مزدوجاً أبداً؛ وفشل السجل لا يعطّل النداءة أبداً.

## 4. حالة الولايات (`/api/wilayas`)

- **المصدر**: سجلّ الولايات الثابت (الأسماء وأرقام الطوارئ) + عدد الحرائق/البقع **محسوبان حصرياً من البلاغات والبقع الحية** — القيم المخزنة في السجل صفرية ومعطّلة.
- **التخزين المؤقت**: 60 ثانية + ترويسة `X-Last-Updated`؛ إعادة الحساب عند كل تغيير بلاغ.

## 5. الطقس — Open-Meteo (في الواجهة)

- **المستخدَم في**: `EvacuationRadar.tsx` (رياح/اتجاه/حرارة لحظة الرصد)
- **المصدر**: `api.open-meteo.com` المجاني بلا مفتاح.
- **عند التعطّل**: محاكاة انجراف رياح واقعي داخل نطاق معروف (وسم `isLive: false`).

## 6. التوجيه — OSRM العام (في الواجهة)

- **المستخدَم في**: `SafeEvacuation.tsx` (حساب زمن/مسافة طريق الإخلاء)
- **المصدر**: `router.project-osrm.org` (خوادم عامة مجانية، لا ضمان توافر).
- **عند التعطّل**: تقدير haversine (معامل 36كم/س) بدل الاستجابة الفاشلة.

## 7. الخرائط — OSM + CARTO

- **الافتراضي (بدون مفتاح)**: بلاطات OpenStreetMap القياسية في كل مكان + OpenTopoMap للتضاريس في مركز القيادة + وضع داكن عبر فلتر CSS — لا مفتاح، لا تسجيل.
- **بمفتاح كارتو (اختياري)**: ضع `CARTO_BASEMAP_KEY` في متغيرات بيئة Render → يخدمه `GET /api/config` → ترتقي خلفيات light/dark (ويب) وخريطة التطبيق (voyager) إلى بلاطات كارتو ذات المفتاح. المفتاح عام بطبيعته (يظهر في كل رابط بلاطة) لذا لا يُعد سراً — لكنه لا يُكتب في الكود أبداً.
- **عند التعطّل**: مربعات مفقودة فقط؛ لا تعطّل وظائف الخريطة (علامات/بيانات محلية). ميتُ الحصة = نعود تلقائياً للافتراضي الخالي من المفتاح.

## 8. الشبكة Mesh / الأحداث الحية

- `GET /api/mesh/token` (حد 10/دقيقة) ثم WebSocket `/ws` لترحيل التقارير بين العقد (إثبات عمل + تشفير IV/ciphertext).
- الأحداث الحية للواجهة: SSE `LiveHub` (تحديثات فورية للبلاغات والمناطق الآمنة).

## 9. التاريخ والتحليلات (`/api/history`)

- **المصدر**: عدّادات يومية محسوبة من بلاغات Firestore (بلا بذرة — تعطّل القاعدة = عدادات صفرية صادقة) + نداءات SOS الذاكرية + بقع FIRMS — لا مخزن تاريخي منفصل؛ المدى الافتراضي 30 يوماً (7..180).
- **مؤشر الخطر**: محسوباً في الواجهة (`src/utils/fireRisk.ts`) من نفس بيانات العرض (بلاغات نشطة + بقع حية + خطورة الولايات) — حتمي وبلا تبعيات خارجية.

## ملاحظة الصدق
لا تصف المنصة بيانات FIRMS بأنها «حية (Live)» — الوثائق والواجهة تستخدمان **Near-real-time**. ومنذ **v2.3.0 (تصفية المحاكاة)** أُزيلت جميع البيانات المحاكاة من المنصة: بلاغات وهمية، بؤر أقمار مُختلقة، فرق إنقاذ وهمية متحركة، وتوجيه بأسماء حرة — كل رقم معروض يأتي من مصدر حقيقي، وإلا ظهر صفراً أو فراغاً بصراحة.
