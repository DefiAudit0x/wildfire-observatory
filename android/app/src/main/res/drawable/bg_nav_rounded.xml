<?xml version="1.0" encoding="utf-8"?>
<!-- v2.20.0 Neo bottom nav: floating slab with 24dp top corners + a
     white/8 top hairline (was a flat full-bleed navy bar). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/nav_bg" />
    <corners android:topLeftRadius="24dp" android:topRightRadius="24dp" />
    <stroke android:width="1dp" android:color="@color/border_subtle" />
</shape>
