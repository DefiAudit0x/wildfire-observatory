<?xml version="1.0" encoding="utf-8"?>
<!-- v2.20.0 Neo risk band pill: red-tinted glow container (color is
     neutral — the band value still comes from code via text color). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/accent_red_container" />
    <corners android:radius="99dp" />
    <stroke android:width="1dp" android:color="@color/accent_red_dim" />
</shape>
