<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Bluetooth Mesh Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 12+ Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- Foreground service + notifications -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Phase 2: the team-location FGS (TeamLocationService) needs the
         location-typed FGS permission on API 34+ in addition to the runtime
         ACCESS_FINE_LOCATION granted for mesh/WebView use. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Internet for the native data sync (same API the web console uses) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera for report images -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- v1.0.4: microphone for the SOS voice message — the WebView's
         getUserMedia({audio}) is granted by the WebChromeClient only when
         the app itself holds this runtime permission. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- GPS for geolocation: the standard runtime permissions above
         (ACCESS_FINE/COARSE_LOCATION) are the real ones — ACCESS_GPS
         and ACCESS_LOCATION are not platform permissions (audit round 11). -->

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".ObservatoryApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="المرصد الجزائري لحرائق الغابات والكوارث"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="36">

        <!-- v2.0.0: PURE NATIVE activity — no WebView anywhere in the app.
             The owner's directive: "تطبيق أندرويد خالص لا تغليف الموقع". -->
        <activity
            android:name=".NativeMainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ARC-M37 fix: dataSync FGS is capped (6h) and killed for background
             starts on Android 15+ — a fatal mismatch for an always-on mesh
             relay. specialUse is the documented type for long-running
             device-essential work; the subtype documents the reason. -->
        <service
            android:name=".MeshService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="offline-mesh-relay-for-wildfire-dispatch" />
        </service>

        <!-- Phase 2: team-member GPS tracking. The location FGS type keeps
             the beat loop legal while the screen is off (while-in-use access,
             service started from the foreground panel). -->
        <service
            android:name=".TeamLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>
</manifest>
